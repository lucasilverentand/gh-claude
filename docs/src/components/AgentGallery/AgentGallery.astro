---
import { AGENTS, CATEGORIES, OUTPUT_LABELS } from '../../data/agents';
import AgentGalleryClient from './AgentGalleryClient';
---

<div class="agent-gallery-wrapper not-content">
  <AgentGalleryClient
    client:load
    agents={AGENTS}
    categories={CATEGORIES}
    outputLabels={OUTPUT_LABELS}
  />
</div>
