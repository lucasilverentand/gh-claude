---
title: Installation
slug: install
description: Step-by-step guide to install Repo Agents CLI
next: ../quick-start
---

## Prerequisites

Before installing Repo Agents, ensure you have:

- **Node.js 18.0.0 or higher** (or Bun)
- **GitHub CLI** (`gh`) installed and authenticated (used for GitHub operations)
- An **Anthropic API key** for Claude

## Usage

Run Repo Agents directly with npx or bunx (no installation required):

```bash
npx @repo-agents/cli init
# or
bunx @repo-agents/cli init
```

### Global Installation (Optional)

If you prefer a global install:

```bash
npm install -g @repo-agents/cli
# or
bun install -g @repo-agents/cli
```

Then run commands directly:

```bash
repo-agents init
```

## Verify

Check the CLI version:

```bash
npx @repo-agents/cli --version
```

## Authentication

Configure Claude API access:

```bash
repo-agents setup-token
```

This guides you through adding your Anthropic API key as a repository secret.

